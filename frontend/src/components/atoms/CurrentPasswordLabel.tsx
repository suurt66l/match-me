export default function CurrentPasswordLabel() {
    return (
        <label htmlFor="password" className="block text-sm font-bold text-gray-100">
            Current password (to confirm)
        </label>
    );
}
